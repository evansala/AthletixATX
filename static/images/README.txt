Put your real image files here, using these exact filenames (the templates already point at them):

- logo-circle.jpg   — the round Kinetix logo used in the nav and footer
- logo-banner.jpg   — the full-screen banner image on the homepage

Until you add them, the browser will just show a broken-image icon in those spots — everything else on the site works fine without them.
